export const PORT = 5000;
export const BASE_URL = `http://localhost:${PORT}`;
export const GLOBAL_SOCKET_NAMESPACE = "/ts-socket"; 
export const SOCKET_URI = `http://` + window.location.hostname + GLOBAL_SOCKET_NAMESPACE;
