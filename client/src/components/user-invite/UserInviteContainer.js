import { connect } from 'react-redux';
import UserInvite from './UserInvite';

const mapStateToProps = (state) => {

};

const mapDispatchToProps = () => {

};

export default connect()(UserInvite);
// Wait... this isn't a redux application